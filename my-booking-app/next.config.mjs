/** @type {import('next').NextConfig} */
const nextConfig = {
    // Hier können bei Bedarf Konfigurationen hinzugefügt werden.
    // Zum Beispiel:
    // reactStrictMode: true,
};

export default nextConfig;